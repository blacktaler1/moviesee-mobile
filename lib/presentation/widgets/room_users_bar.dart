import 'package:flutter/material.dart';

class RoomUsersBar extends StatelessWidget {
  const RoomUsersBar({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
